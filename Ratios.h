#pragma once

enum
{
    milli_denominator = 1000,
    micro_denominator = milli_denominator * 1000,
    nano_denominator = micro_denominator * 1000,
    secs_in_min = 60,
    secs_in_hr = secs_in_min * 60
};
